<UserControl x:Class="loadify.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="850">
    <Grid>
        <Menu BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="0 0 0 1">
            <MenuItem Header="General" Style="{StaticResource SpotifyMenuItemHeading}">
                <MenuItem Header="About" Style="{StaticResource SpotifyMenuItemHeading}"/>
                <MenuItem Header="Help" Style="{StaticResource SpotifyMenuItemHeading}"/>
            </MenuItem>
            <MenuItem Header="Settings" Style="{StaticResource SpotifyMenuItemHeading}"/>
        </Menu>

    </Grid>
</UserControl>
