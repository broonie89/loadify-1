<UserControl x:Class="loadify.View.DownloaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisiblityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="77*"/>
        </Grid.ColumnDefinitions>
        <ProgressBar x:Name="Progress" Visibility="{Binding Active, Converter={StaticResource BoolToVisiblityConverter}}" Foreground="{StaticResource AccentColorBrush}" Background="Transparent" Height="Auto" Margin="10 8"/>
        <Label Grid.Column="1" Visibility="{Binding Active, Converter={StaticResource BoolToVisiblityConverter}}" Foreground="#FF7D7D7D"/>
    </Grid>
</UserControl>
